namespace ADL.Core
{
    /// <summary>
    /// Enumeration of the types of death penalty in the game
    /// </summary>
    public enum DeathPenaltyType
    {
        Normalcore,
        Softcore,
        Hardcore,
    }
}